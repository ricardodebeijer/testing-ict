import { MemberListComponent } from './member-list.component';

describe('MemberListComponent', () => {
  // let component: MemberListComponent;


  beforeEach(() => {
    // component = new MemberListComponent();
  });

  it('should create', () => {
    // expect(component).toBeTruthy();
  });
});
